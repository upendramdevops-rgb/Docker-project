apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: devopsapp

spec:
  replicas: 2

  selector:
    matchLabels:
      app: db

  serviceName: devopsdb

  template:
    metadata:
      labels:
        app: db

    spec:
      containers:
        - name: cont-1
          image: upendra9010/k8sfirst:dbimage
          ports:
            - containerPort: 3306

      imagePullSecrets:
        - name: flm

---
apiVersion: v1
kind: Service

metadata:
  name: devopsdb

spec:
  clusterIP: None

  selector:
    app: db

  ports:
    - port: 3306
      targetPort: 3306
